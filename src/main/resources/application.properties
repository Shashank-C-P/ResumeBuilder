spring.application.name=ResumeBuilder

Server configuration for proxy environments (like Railway)
server.forward-headers-strategy=NATIVE

--- FINAL DATABASE CONFIGURATION ---
We are now using the specific internal network host provided by Railway.
spring.datasource.url=jdbc:mysql://mysql.railway.internal:DB 
P
​
 ORT/{DB_NAME}
spring.datasource.username=DB 
U
​
 SERspring.datasource.password={DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

Connection timeout settings to make the app more patient
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=0

JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

JSP View Configuration
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

logging.level.org.springframework.security=DEBUG